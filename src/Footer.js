import './App.css';
import { FooterContainer, FooterLinks } from './components';

export const Footer = (props) => {

  return (
    <div>
      <FooterContainer>
        <FooterLinks/>
      </FooterContainer>
    </div>
  )
}
