import './App.css';
import { FooterAddress, FooterContainer, FooterForm } from './components';

export const Footer = (props) => {

  return (
    <FooterContainer>
      <FooterAddress />
      <FooterAddress />
      <FooterForm />
    </FooterContainer>

  )
}
