

export const FooterContainer = ({props, children}) => {
    
  return (
    <footer class="bg-white flex flex-row items-center justify-around h-80 w-screen m-3 font-['Open_Sans']">
      {children}
    </footer>
  )
}